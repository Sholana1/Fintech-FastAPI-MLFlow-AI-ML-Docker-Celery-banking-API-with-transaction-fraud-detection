services:
  postgres:
    build:
      context: .
      dockerfile: ./backend/docker/local/postgres/Dockerfile
    ports:
      - "5432:5432"
    volumes:
      - flowbank_local_db:/var/lib/postgresql/data
    env_file:
      - ./.envs/.env.local
    networks:
      - flowbank_local_nw
    
networks:
  flowbank_local_nw:
    external: true

volumes:
  flowbank_local_db: